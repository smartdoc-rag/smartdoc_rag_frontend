export interface User {
    id: string,
    email: string,
    fullName: string,
    isActive: string,
    role: string
}

export interface UserResponse {
    id: number,
    email: string,
    full_name: string,
    is_active: string,
    role: string
}